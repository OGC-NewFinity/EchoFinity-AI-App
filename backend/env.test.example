# Test Environment Variables
# Copy this file to .env.test for local testing
# CI uses these values via GitHub Actions environment variables

NODE_ENV=test

# JWT Configuration
JWT_SECRET=test-secret-key-for-jwt-signing

# Database Configuration (not used in tests, all mocked)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=echofinity_test
DB_USER=test_user
DB_PASSWORD=test_password

# Redis Configuration (not used in tests, all mocked)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=

# AI Service Configuration (not used in tests, all mocked)
AI_SERVICE_URL=http://localhost:8001

# Server Configuration
PORT=4000
